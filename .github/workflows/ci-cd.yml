name: Java CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  release:
    types: [ published ]

env:
  JAVA_VERSION: '17'
  GRADLE_VERSION: '8.7'

jobs:

  # ─────────────────────────────────────────────────────────────
  # JOB 1: Build & Test
  # ─────────────────────────────────────────────────────────────
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Run full build with tests
        run: ./gradlew clean build

      - name: Generate JaCoCo coverage report
        run: ./gradlew jacocoTestReport

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: build/reports/tests/test/

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: build/reports/jacoco/test/html/

  # ─────────────────────────────────────────────────────────────
  # JOB 2: Code Quality Check
  # ─────────────────────────────────────────────────────────────
  code-quality:
    name: Code Quality
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Verify coverage threshold (70%)
        run: ./gradlew jacocoTestCoverageVerification

  # ─────────────────────────────────────────────────────────────
  # JOB 3: Package as Fat JAR
  # ─────────────────────────────────────────────────────────────
  package:
    name: Package Application
    runs-on: ubuntu-latest
    needs: [ build-and-test, code-quality ]

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build fat JAR
        run: ./gradlew fatJar

      - name: Upload fat JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-fat-jar
          path: build/libs/*-all.jar
          retention-days: 30

# ── COMMENTED OUT — requires Docker Hub and staging server secrets ──────────
#
#  docker-build:
#    name: Docker Build & Push
#    runs-on: ubuntu-latest
#    needs: package
#    if: github.ref == 'refs/heads/main'
#
#    steps:
#      - name: Checkout source code
#        uses: actions/checkout@v4
#
#      - name: Set up JDK ${{ env.JAVA_VERSION }}
#        uses: actions/setup-java@v4
#        with:
#          java-version: ${{ env.JAVA_VERSION }}
#          distribution: 'temurin'
#
#      - name: Cache Gradle packages
#        uses: actions/cache@v4
#        with:
#          path: |
#            ~/.gradle/caches
#            ~/.gradle/wrapper
#          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}
#
#      - name: Make gradlew executable
#        run: chmod +x ./gradlew
#
#      - name: Build fat JAR for Docker
#        run: ./gradlew fatJar
#
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3
#
#      - name: Log in to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      - name: Extract version from gradle
#        id: version
#        run: echo "VERSION=$(./gradlew properties -q | grep '^version:' | awk '{print $2}')" >> $GITHUB_OUTPUT
#
#      - name: Build and push Docker image
#        uses: docker/build-push-action@v5
#        with:
#          context: .
#          file: ./docker/Dockerfile
#          push: true
#          tags: |
#            ${{ secrets.DOCKER_USERNAME }}/java-devops-project:latest
#            ${{ secrets.DOCKER_USERNAME }}/java-devops-project:${{ steps.version.outputs.VERSION }}
#          cache-from: type=gha
#          cache-to: type=gha,mode=max
#
#  deploy-staging:
#    name: Deploy to Staging
#    runs-on: ubuntu-latest
#    needs: docker-build
#    if: github.ref == 'refs/heads/main'
#    environment: staging
#
#    steps:
#      - name: Notify deployment start
#        run: echo "Deploying version to staging environment..."
#
#      - name: Deploy via SSH
#        uses: appleboy/ssh-action@v1.0.3
#        with:
#          host: ${{ secrets.STAGING_HOST }}
#          username: ${{ secrets.STAGING_USER }}
#          key: ${{ secrets.STAGING_SSH_KEY }}
#          script: |
#            docker pull ${{ secrets.DOCKER_USERNAME }}/java-devops-project:latest
#            docker stop java-devops-app || true
#            docker rm java-devops-app || true
#            docker run -d \
#              --name java-devops-app \
#              --restart unless-stopped \
#              -p 8080:8080 \
#              ${{ secrets.DOCKER_USERNAME }}/java-devops-project:latest
#            echo "Staging deployment complete"